init : 현재 폴더를 깃의 로컬 저장소로 만들어준다.

add : 현재 파일을 깃의 원격 저장소에 올려줄 준비를 한다.

commit : add 한 파일의 변경사항을 저장한다.

push : 깃의 원격저장소에 add되어있는 파일을 커밋과 함께 올린다.

branch : 줄기

merge : 합체


init -> add -> commit x 100 (직접) -> push 끗

